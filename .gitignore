# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables
# Amplo de proposito: pega .env, .env.production, backups (.env.local.bak-*)
# e qualquer variante futura. O .example fica de fora pra continuar versionado.
.env
.env.*
!.env.example
!.env.local.example

# Common ignores
node_modules
.next/
.DS_Store

# Build artifacts
tsconfig.tsbuildinfo

# Local tooling state
.claude/settings.local.json
supabase/.temp/
# Fontes de design (PSD/ZIP) — pesadas e sem diff útil. Os assets exportados
# que o app usa ficam versionados em public/.
*.psd
*.zip
