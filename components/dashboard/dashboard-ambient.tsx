/**
 * Background do dashboard.
 *
 * Sistema Linear (voltagent/awesome-design-md · linear.app/DESIGN.md):
 * o canvas escuro É o espaço em branco. Sem gradientes atmosféricos, glows,
 * spotlights ou grid — a profundidade vem da escada de superfícies + hairlines
 * definida em app/dashboard/dashboard.css. Componente mantido como no-op pra
 * não alterar a árvore do layout.
 *
 * Já teve neve de pixels (PixelSnow/WebGL) por aqui em 20/08/2026; saiu no
 * mesmo dia porque a animação não agradou. Está no histórico do git se um dia
 * a ideia voltar.
 */
export function DashboardAmbient() {
  return null
}
